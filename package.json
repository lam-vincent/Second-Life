{
  "homepage": "https://lam-vincent.github.io/depot-sauvage",
  "scripts": {
    "predeploy": "cd client && npm install && npm run build",
    "deploy": "gh-pages -d client/dist"
  },  
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
